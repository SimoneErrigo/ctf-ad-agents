apiVersion: v1
kind: Service
metadata:
  name: agent-chat-ui
  labels:
    app: agent-chat-ui
spec:
  selector:
    app: agent-chat-ui
  ports:
    - name: http
      port: 3000
      targetPort: 3000
