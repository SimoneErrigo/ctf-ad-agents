# The patcher-mcp image only needs pyproject.toml, README.md and patcher_mcp/
# (see Dockerfile). Keep the local dev venv and caches out of the build context.
.venv
**/.venv
__pycache__
**/__pycache__
*.pyc
.ruff_cache
.mypy_cache
.pytest_cache
*.egg-info
.DS_Store
